local options = {
  ensure_installed = { "c", "cpp", "lua", "vim", "vimdoc", "query", "python", "javascript" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },

  auto_install = true,
}

return options
